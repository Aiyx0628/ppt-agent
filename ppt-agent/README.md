# ppt-agent

Python backend service for the workspace.

## Runtime

- Python `3.13.12`
- Dependency management via `uv`
- Source layout via `src/`

## Commands

```bash
uv sync
uv run ppt-agent
uv run python -m ppt_agent
```
